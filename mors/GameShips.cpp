#include "GameShips.h"

Ship4::Ship4(int index) : Ship()
{
	_index = index;
}

Ship3::Ship3(int index) : Ship()
{
	_index = index;
}

Ship2::Ship2(int index) : Ship()
{
	_index = index;
}

Ship1::Ship1(int index) : Ship()
{
	_index = index;
}
